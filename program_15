#include<stdio.h>
int main()
{
    int n,i=1,fac=1;
    printf("Enter n:");
    scanf("%d",&n);
    while(i<=n)
    {
        fac=fac*i;
        i++;
    }
    printf("factorial is %d",fac);
    return 0;
}
